Store screenshots go here, 2000×1250, named `seo-audit-1.png` and so on.

They have to be captured from Raycast with the extension running — `ray develop`
loads it, and the store shows these on the extension's page. Deliberately not
mock-ups: everything else this project shows is a real window.
